stage = "local_training"
experiment_name = "global_centralized_tabnet"
output_dir = "data/experiments"
model_config = "configs/shared/model.toml"

institution_id = "global"
dataset_path = "data/train_test_splits/global_train.csv"

local_epochs = 5
learning_rate = 0.02
fraud_weight = 8
batch_size = 256
classification_threshold = 0.5
seed = 42
